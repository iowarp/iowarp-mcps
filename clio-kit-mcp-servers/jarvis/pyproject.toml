[project]
name = "jarvis-mcp"
version = "1.0.1"
description = "Jarvis-CD MCP - Pipeline Management for High-Performance Computing with comprehensive workflow operations"
readme = "README.md"
requires-python = ">=3.10"
license = "BSD-3-Clause"
authors = [
    { name = "IoWarp Team - Gnosis Research Center", email = "grc@illinoistech.edu" }
]

keywords = ["jarvis", "pipeline-management", "high-performance-computing", "hpc", "workflow", "data-pipelines", "scientific-computing", "mcp", "package-management"]

dependencies = [
  "fastmcp>=3.2.0",
  "fastapi",
  "python-dotenv>=1.2.2",
  "jarvis-cd @ git+https://github.com/grc-iit/jarvis-cd.git@main#egg=jarvis-cd",
  "starlette>=1.3.1",
  "authlib>=1.6.12",
  "cryptography>=48.0.1",
  "idna>=3.15",
  "pydantic-settings>=2.14.2",
  "pygments>=2.20.0",
  "pyjwt>=2.13.0",
  "python-multipart>=0.0.31",
  "urllib3>=2.7.0",
]

[project.urls]
Homepage = "https://docs.iowarp.ai/"
Repository = "https://github.com/iowarp/clio-kit"

[project.scripts]
jarvis-mcp = "jarvis_mcp.user_server:main"
jarvis-admin-mcp = "jarvis_mcp.admin_server:main"

[tool.uv]
dev-dependencies = [
    "pytest>=9.0.3",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=6.2.1",
    "pytest-timeout>=2.4.0",
    "ruff>=0.12.5",
    "psutil>=6.0.0",
    "mypy>=1.17.0",
    "types-requests",
    "pip-audit>=2.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/jarvis_mcp"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
